{
  "name": "claude-replay",
  "version": "0.1.1",
  "description": "Community tool to convert Claude Code session transcripts into self-contained, embeddable HTML replays",
  "type": "module",
  "bin": {
    "claude-replay": "bin/claude-replay.mjs"
  },
  "files": [
    "bin/",
    "src/",
    "template/"
  ],
  "scripts": {
    "build": "node scripts/build-template.mjs",
    "test": "node --test test/"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "claude",
    "claude-code",
    "replay",
    "transcript",
    "html"
  ],
  "license": "MIT"
}
